<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- <record id="res_partner_tree_inherit_dealer" model="ir.ui.view">
    <field name="name">res.partner.tree.inherit.dealer</field>
    <field name="model">res.partner</field>
    <field name="inherit_id" ref="base.view_partner_tree"/>
    <field name="arch" type="xml">

        <xpath expr="//field[@name='complete_name']" position="after">
            <field name="is_dealer" invisible="1"/>
        </xpath>

        <xpath expr="//list/field[@name='complete_name']" position="replace">
            <field name="complete_name"/>
            <field name="is_dealer" invisible="1"/>
        </xpath>

        <xpath expr="//list" position="inside">
            <templates>
                <t t-name="dealer_badge_row" t-inherit-mode="extension">
                    <xpath expr="//field[@name='complete_name']" position="after">
                        <t t-if="record.is_dealer.raw_value">
                            <span class="badge bg-success ms-1">Dealer</span>
                        </t>
                    </xpath>
                </t>
            </templates>
        </xpath>

    </field>
</record> -->

<record id="res_partner_kanban_inherit_dealer" model="ir.ui.view">
    <field name="name">res.partner.kanban.inherit.dealer</field>
    <field name="model">res.partner</field>
    <field name="inherit_id" ref="base.res_partner_kanban_view"/>
    <field name="arch" type="xml">

        <xpath expr="//kanban" position="inside">
            <field name="is_dealer"/>
        </xpath>

        <!-- YOUR BADGE -->
        <xpath expr="//t[@t-name='card']" position="inside">
            <t t-if="record.is_dealer and record.is_dealer.raw_value">
                <div class="position-absolute top-0 end-0 m-1">
                    <span class="badge bg-success">Dealer</span>
                </div>
            </t>
        </xpath>

    </field>
</record>
    

</odoo>
